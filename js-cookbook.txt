var			number,"string",true,false,null,undefined;

==	===	 >	>=	<	<=	!=	!==			logical/comparision operators

alert();

prompt();

console.log(); 

function function_name(argument) {
	// body...
}

array	["", "", "", ""];
array.push()-----to add entry to the end of array
array.unshift()-----to add entry to the start of array
array.pop()-----to get entry out from the end of array
array.shift()-----to get entry out from the start of array
array.indexOf("")-----to check the string exist in array or not
array.slice()-----to remove/cut entry from the array
array.length-----to get the length of the array

object{
	property: "value",
}

if (true) {} else {}


for (var i = 0; i < Things.length; i++) {
	Things[i]
}

var i=0
while(i< Things.length){
	Things[i];
	i++;
}

document.getElementsByTagName('tagName')
document.getElementById('idName')
document.querySelector("#idName or tagName")
document.querySelectorAll(".className or tagName")

var.textContent		to change text content
var.innerHTML		to change text content including HTML tags

var.addEventListner("click or change or hover", function function_name(argument) {
	// body...
});

var.style.cssProperty = value;

var.classList.add("classNameinCSS");
var.classList.remove("classNameinCSS");
var.classList.toggle("classNameinCSS");

Math.random() * (num+1);		to generate random numbers num=limit upto you want to generate random numbers
Math.floor();					to round off the fractional numbers,Rounds Off(thats why +1 in random number limit)